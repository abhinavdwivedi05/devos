# DevOS Environment Variables Example
# This project runs entirely on frontend mock services and client-side Zustand stores.
# No active third-party developer secrets or database keys are required to run/boot the app.

# Optional development server configuration
PORT=3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
